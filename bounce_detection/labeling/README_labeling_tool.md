# 半自动标注工具

**Semi-automatic Labeling Tool for Bounce Detection**

基于 Phase 1 规则检测结果的交互式标注工具，用于生成 BounceNet 训练数据。

> 📖 相关文档:
> - [模块总览](../README.md)
> - [Phase 1 规则检测](../README_phase1_rules.md)
> - [Phase 2 BounceNet](../README_phase2_bouncenet.md)

---

## 📋 目录

- [功能特性](#功能特性)
- [快速开始](#快速开始)
- [使用指南](#使用指南)
- [界面说明](#界面说明)
- [快捷键](#快捷键)
- [数据格式](#数据格式)
- [批量标注](#批量标注)
- [导出训练数据](#导出训练数据)
- [API 参考](#api-参考)

---

## 功能特性

### 核心功能

- ✅ **Phase 1 预填充**: 自动运行规则检测，预填充候选事件
- ✅ **视频帧浏览**: 支持视频帧和轨迹同步显示
- ✅ **事件编辑**: 确认/删除/修改/添加事件
- ✅ **实时统计**: 显示标注进度和事件分布
- ✅ **自动保存**: JSON 格式标注文件
- ✅ **批量处理**: 支持整个 match 目录的连续标注
- ✅ **数据导出**: 导出为 CSV 训练格式

### 事件类型

| 类型 | 标识 | 颜色 | 快捷键 |
|------|------|------|--------|
| 落地点 | `landing` | 🔴 红色 | L |
| 击球点 | `hit` | 🔵 蓝色 | H |
| 出画面 | `out_of_frame` | ⬜ 灰色 | O |
| 其他 | `other` | 🟢 绿色 | N |

---

## 快速开始

### 安装依赖

```bash
pip install numpy pandas matplotlib opencv-python
```

### 标注单个文件

```bash
# 基本用法（自动查找对应视频）
python labeling_launcher.py --csv data/test/match1/csv/1_05_02_ball.csv

# 指定视频文件
python labeling_launcher.py --csv data/test/match1/csv/1_05_02_ball.csv \
                            --video data/test/match1/video/1_05_02.mp4

# 加载已有标注继续编辑
python labeling_launcher.py --csv data/test/match1/csv/1_05_02_ball.csv \
                            --labels data/test/match1/labels/1_05_02_labels.json
```

### 批量标注

```bash
# 标注整个 match 目录
python labeling_launcher.py --match_dir data/test/match1
```

---

## 使用指南

### 工作流程

```
1. 启动工具
   └── 自动加载 CSV 轨迹
   └── 自动查找对应视频
   └── 自动运行 Phase 1 检测预填充
   
2. 浏览候选事件
   └── ↑/↓ 跳转到上/下一个事件
   └── ←/→ 逐帧浏览
   
3. 确认/修正事件
   └── Y 确认当前事件
   └── D 删除误检事件
   └── L/H/O 修改事件类型
   
4. 补充遗漏事件
   └── 导航到遗漏的帧
   └── 按 N 添加新事件
   └── 按 L/H/O 设置类型
   
5. 保存标注
   └── 按 S 保存
   └── 按 Q 退出
```

### 标注标准

#### Landing (落地点)
- 球落地后停止或消失
- 典型特征：速度骤降至接近零

#### Hit (击球点)
- 球被球拍击中的瞬间
- 典型特征：速度方向突然反转，速度大小保持较高

#### Out-of-frame (出画面)
- 球飞出画面边缘
- 典型位置：画面顶部/底部/两侧边缘

---

## 界面说明

```
┌─────────────────────────────────────────────────────────────────┐
│                    Bounce Detection Labeling Tool               │
├───────────────────────────────┬─────────────┬───────────────────┤
│                               │             │                   │
│       视频帧 / 轨迹图          │   信息面板   │    事件列表       │
│                               │             │                   │
│   - 显示当前帧                 │ - 当前帧号   │ - 所有事件        │
│   - 轨迹线（绿色）             │ - 当前事件   │ - ✓ 已确认        │
│   - 球位置（黄色）             │ - 统计信息   │ - ○ 未确认        │
│   - 事件标记                   │ - 快捷键    │ - ▶ 当前选中      │
│                               │             │                   │
├───────────────────────────────┴─────────────┴───────────────────┤
│                                                                 │
│     Y 坐标图              │      速度图        │     按钮区域     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 图标说明

| 标记 | 含义 |
|------|------|
| 🟡 黄色圆圈 | 当前帧球的位置 |
| ⭐ 红色星号 | 落地点事件 |
| 🔵 蓝色圆圈 | 击球点事件 |
| ✕ 灰色叉号 | 出画面事件 |
| 实心图标 | 已确认事件 |
| 空心图标 | 未确认事件 |

---

## 快捷键

### 导航

| 快捷键 | 功能 |
|--------|------|
| `←` / `A` | 上一帧 |
| `→` / `D` | 下一帧 |
| `↑` / `W` | 跳转到上一个事件 |
| `↓` | 跳转到下一个事件 |
| `Home` | 跳转到第一帧 |
| `End` | 跳转到最后一帧 |
| `PageUp` | 后退 30 帧 |
| `PageDown` | 前进 30 帧 |

### 事件编辑

| 快捷键 | 功能 |
|--------|------|
| `Y` | 确认当前事件 |
| `Delete` / `D` | 删除当前事件 |
| `L` | 设置/添加为 Landing |
| `H` | 设置/添加为 Hit |
| `O` | 设置/添加为 Out-of-frame |
| `N` | 添加为 Other 类型 |

### 系统

| 快捷键 | 功能 |
|--------|------|
| `S` | 保存标注 |
| `Q` / `Esc` | 退出 |

---

## 数据格式

### 标注文件 (JSON)

```json
{
  "metadata": {
    "video_path": "data/test/match1/video/1_05_02.mp4",
    "csv_path": "data/test/match1/csv/1_05_02_ball.csv",
    "total_frames": 517,
    "fps": 30,
    "labeled_at": "2026-01-21T15:30:00"
  },
  "events": [
    {
      "frame": 30,
      "x": 481.0,
      "y": 385.0,
      "event_type": "hit",
      "rule": "vy_reversal",
      "confidence": 0.8,
      "confirmed": true,
      "features": {
        "vy_before": -21.0,
        "vy_after": 179.0
      }
    },
    {
      "frame": 516,
      "x": 652.0,
      "y": 698.0,
      "event_type": "landing",
      "rule": "trajectory_end",
      "confidence": 0.7,
      "confirmed": true,
      "features": {}
    }
  ]
}
```

### 导出格式 (CSV)

```csv
rally,frame,x,y,event_type,confidence
1_05_02,30,481.0,385.0,hit,1.0
1_05_02,72,718.0,436.0,hit,1.0
1_05_02,516,652.0,698.0,landing,1.0
```

---

## 批量标注

### 批量标注流程

```bash
# 1. 启动批量标注
python labeling_launcher.py --match_dir data/test/match1

# 2. 自动显示待标注文件列表
# Found 11 CSV files
# Labeled: 0
# Pending: 11

# 3. 逐个标注
# - 每个文件标注完成后自动保存
# - 询问是否继续下一个

# 4. 标注结果保存到 data/test/match1/labels/
```

### 指定输出目录

```bash
python labeling_launcher.py --match_dir data/test/match1 \
                            --output_dir my_labels/match1
```

---

## 导出训练数据

### 导出所有已确认事件

```bash
python labeling_launcher.py --export data/test/match1/labels \
                            --output training_events.csv
```

### 查看标注统计

```bash
python labeling_launcher.py --stats data/test/match1/labels
```

输出示例：
```
============================================================
Labeling Statistics
============================================================
Total label files: 11
Total events: 101
Confirmed events: 85
Confirmation rate: 84.2%

By event type:
  hit: 62
  landing: 12
  out_of_frame: 11

By rally:
  1_05_02: 10/13 confirmed
  1_05_03: 8/11 confirmed
  ...
```

---

## API 参考

### LabelingDataManager

```python
from bounce_detection.labeling import LabelingDataManager

# 初始化
dm = LabelingDataManager(
    video_path='path/to/video.mp4',  # 可选
    csv_path='path/to/trajectory.csv',
    label_path='path/to/labels.json'  # 可选
)

# 访问数据
frame = dm.get_frame(100)           # 获取第 100 帧
x, y, vis = dm.get_trajectory_at_frame(100)  # 获取轨迹点

# 事件管理
dm.add_event({'frame': 100, 'event_type': 'hit'})
dm.confirm_event(100)
dm.update_event(100, {'event_type': 'landing'})
dm.remove_event(100)

# 保存
dm.save_labels('output.json')

# 统计
stats = dm.get_statistics()
```

### LabelingTool

```python
from bounce_detection.labeling import LabelingTool

# 初始化并启动
tool = LabelingTool(
    csv_path='path/to/trajectory.csv',
    video_path='path/to/video.mp4',  # 可选
    auto_detect=True  # 是否自动运行 Phase 1
)

# 启动交互界面
events = tool.run()
```

### LabelExporter

```python
from bounce_detection.labeling.data_manager import LabelExporter

# 导出为训练 CSV
LabelExporter.to_training_csv(dm, 'output.csv', only_confirmed=True)

# 导出事件列表
LabelExporter.to_event_list(dm, 'events.csv', only_confirmed=True)
```

---

## 文件结构

```
bounce_detection/labeling/
├── __init__.py              # 模块入口
├── data_manager.py          # 数据加载/保存/管理
├── visualizer.py            # 可视化组件
├── labeling_tool.py         # 主标注工具
└── README.md                # 本文档

labeling_launcher.py         # 启动脚本
test_labeling_tool.py        # 测试脚本
```

---

## 更新日志

### v0.1.0 (2026-01-21)

- ✅ 实现 LabelingDataManager 数据管理
- ✅ 实现 matplotlib 交互界面
- ✅ 支持 Phase 1 自动预填充
- ✅ 支持键盘快捷键操作
- ✅ 支持批量标注模式
- ✅ 支持 CSV/JSON 导出
